export type Banner = {
  picture: string;
};

export type Service = {
  nama: string;
};

export type CCabang = {
  alamat: string;
  closeHour: string;
  nama: string;
  openHour: string;
  phoneNumber: string;
  picture: string;
};
